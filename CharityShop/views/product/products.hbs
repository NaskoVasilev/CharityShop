<section class="features_causes" id="products">
    <div class="container">
        <div class="row">

            {{#each products}}
                <div class="col-lg-4 col-md-6">
                    <div class="card">
                        <div class="card-body">
                            <figure>
                                <img class="card-img-top img-fluid" src="{{image}}" alt="Product image">
                            </figure>
                            <div class="card_inner_body">
                                <h4 class="card-title">{{name}}</h4>
                                <div class="d-flex justify-content-between raised_goal">
                                    <p><span>Price: {{price}}lv</span></p>
                                </div>
                                <div class="d-flex justify-content-between donation align-items-center">
                                    <a href="/product/buy/{{_id}}" class="primary_btn">Buy</a>
                                    <a href="/product/details/{{_id}}" class="primary-btn blue">Details</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            {{/each}}
        </div>
    </div>
</section>